# Copy to .env and fill in. Never commit .env — it is gitignored.

# --- Binance ---------------------------------------------------------------
# Testnet keys come from https://testnet.binance.vision (sign in with GitHub).
# They are a SEPARATE pair from production keys and are not interchangeable.
#
# On the production key, in the Binance API management screen:
#   ENABLE  "Enable Spot & Margin Trading"
#   DISABLE "Enable Withdrawals"      <- non-negotiable
#   DISABLE "Enable Futures"
#   RESTRICT access to your server's IP
BINANCE_API_KEY=
BINANCE_API_SECRET=

# true  = testnet.binance.vision, fake money (start here, stay a while)
# false = api.binance.com, real money
BINANCE_TESTNET=true

# Second safety catch, independent of the host. true = decide and log but send
# no orders. Set false only when you have read a few hundred journal decisions
# and agree with them.
DRY_RUN=true

# --- Claude ----------------------------------------------------------------
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-5
# low | medium | high | xhigh | max
CLAUDE_EFFORT=medium

# --- Market scope ----------------------------------------------------------
CANDLE_INTERVAL=15m
CANDLES_PER_SYMBOL=120
UNIVERSE_SIZE=12
MIN_QUOTE_VOLUME=20000000

# --- Risk ------------------------------------------------------------------
# These three are coupled: notional = RISK_PER_TRADE / stop_distance.
# Raising RISK_PER_TRADE without raising MAX_POSITION_PCT means the notional
# cap silently binds on every trade. See the comments in config.py.
RISK_PER_TRADE=0.005
MAX_POSITION_PCT=0.30
MAX_DAILY_LOSS_PCT=0.03
MAX_CONCURRENT_POSITIONS=3
MIN_REWARD_RISK=1.2
MAX_STOP_DISTANCE_PCT=0.08
MIN_CONFIDENCE=0.6

# --- Runtime ---------------------------------------------------------------
POLL_SECONDS=30
JOURNAL_PATH=journal.jsonl
STATE_PATH=state.json
